{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "v2:serve": "yarn workspace thinkphp-apidoc-v2 serve",
    "v3:serve": "yarn workspace thinkphp-apidoc-v3 serve",
    "build": "lerna run build --parallel"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "cpx": "^1.5.0"
  }
}
