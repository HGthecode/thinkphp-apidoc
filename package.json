{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "v1:serve": "yarn workspace thinkphp-apidoc-v1 serve",
    "v2:serve": "yarn workspace thinkphp-apidoc-v2 serve",
    "build": "lerna run build --parallel"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "dependencies": {
    "cpx": "^1.5.0"
  }
}
